<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qqsystem.serve.mapper.DailyStatsMapper">
    <insert id="insertOrUpdate" parameterType="com.qqsystem.serve.entity.DailyStats">
        INSERT INTO daily_stats (
            stat_date, pv, uv, new_user_count, user_total_count, 
            drama_count, content_count, view_count, like_count, comment_count, 
            favorite_count, share_count, create_time, update_time
        ) VALUES (
            #{statDate}, #{pv}, #{uv}, #{newUserCount}, #{userTotalCount}, 
            #{dramaCount}, #{contentCount}, #{viewCount}, #{likeCount}, #{commentCount}, 
            #{favoriteCount}, #{shareCount}, NOW(), NOW()
        ) ON DUPLICATE KEY UPDATE
            pv = #{pv},
            uv = #{uv},
            new_user_count = #{newUserCount},
            user_total_count = #{userTotalCount},
            drama_count = #{dramaCount},
            content_count = #{contentCount},
            view_count = #{viewCount},
            like_count = #{likeCount},
            comment_count = #{commentCount},
            favorite_count = #{favoriteCount},
            share_count = #{shareCount},
            update_time = NOW()
    </insert>
    
    <select id="countPvByDate" parameterType="String" resultType="Integer">
        SELECT COUNT(*) FROM user_behavior 
        WHERE DATE(create_time) = #{date}
    </select>
    
    <select id="countUvByDate" parameterType="String" resultType="Integer">
        SELECT COUNT(DISTINCT user_id) FROM login_log 
        WHERE DATE(login_time) = #{date}
    </select>
    
    <select id="countNewUserByDate" parameterType="String" resultType="Integer">
        SELECT COUNT(*) FROM user 
        WHERE DATE(create_time) = #{date}
    </select>
    
    <select id="countTotalUser" resultType="Integer">
        SELECT COUNT(*) FROM user
    </select>
    
    <select id="countDrama" resultType="Integer">
        SELECT COUNT(*) FROM drama
    </select>
    
    <select id="countContent" resultType="Integer">
        SELECT COUNT(*) FROM content
    </select>
    
    <select id="countViewCount" resultType="Integer">
        SELECT COALESCE(SUM(view_count), 0) FROM content
    </select>
    
    <select id="countBehaviorByDateAndAction" resultType="Integer">
        SELECT COUNT(*) FROM user_behavior 
        WHERE DATE(create_time) = #{date} AND action = #{action}
    </select>
    
    <!-- 获取剧目热度趋势 -->
    <select id="selectDramaHeatTrend" parameterType="int" resultType="java.util.Map">
        SELECT 
            stat_date as date, 
            view_count as visitCount, 
            favorite_count as collectCount, 
            like_count as shareCount 
        FROM daily_stats 
        WHERE stat_date >= DATE_SUB(NOW(), INTERVAL #{days} DAY)
        ORDER BY stat_date ASC
    </select>
</mapper>