<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qqsystem.serve.mapper.BehaviorMapper">

    <insert id="insert">
        INSERT INTO ${tableName} (
            user_id,
            target_type,
            target_id
        ) VALUES (
                     #{behavior.userId},
                     #{behavior.targetType},
                     #{behavior.targetId}
                 )
    </insert>

    <delete id="delete">
        DELETE FROM ${tableName}
        WHERE user_id = #{userId}
          AND target_type = #{targetType}
          AND target_id = #{targetId}
    </delete>

    <select id="select" resultType="com.qqsystem.serve.entity.Behavior">
        SELECT *
        FROM ${tableName}
        WHERE user_id = #{userId}
          AND target_type = #{targetType}
          AND target_id = #{targetId}
    </select>

    <select id="selectList" resultType="com.qqsystem.serve.entity.Behavior">
        SELECT *
        FROM ${tableName}
        WHERE user_id = #{userId}
        <if test="targetType != null and targetType != ''">
            AND target_type = #{targetType}
        </if>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countList" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM ${tableName}
        WHERE user_id = #{userId}
        <if test="targetType != null and targetType != ''">
            AND target_type = #{targetType}
        </if>
    </select>

</mapper>
