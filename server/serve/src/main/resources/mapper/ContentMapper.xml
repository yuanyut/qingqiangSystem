<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qqsystem.serve.mapper.ContentMapper">

    <select id="selectById" resultType="com.qqsystem.serve.entity.Content">
        SELECT *
        FROM content
        WHERE id = #{id}
    </select>

    <select id="selectCultureList" resultType="com.qqsystem.serve.entity.Content">
        SELECT id, title, cover, create_time, category
        FROM content
        WHERE biz_type = 'culture'
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countCultureList" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM content
        WHERE biz_type = 'culture'
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
    </select>

    <select id="selectCultureDetail" resultType="com.qqsystem.serve.entity.Content">
        SELECT id, title, content, cover, create_time, category
        FROM content
        WHERE id = #{id} AND biz_type = 'culture'
    </select>

    <insert id="insert" parameterType="com.qqsystem.serve.entity.Content">
        INSERT INTO content (
            title,
            biz_type,
            biz_id,
            type,
            content,
            media_url,
            cover,
            view_count,
            like_count,
            category
        ) VALUES (
                     #{title},
                     #{bizType},
                     #{bizId},
                     #{type},
                     #{content},
                     #{mediaUrl},
                     #{cover},
                     #{viewCount},
                     #{likeCount},
                     #{category}
                 )
    </insert>

    <update id="updateById" parameterType="com.qqsystem.serve.entity.Content">
        UPDATE content
        SET
            title = #{title},
            biz_type = #{bizType},
            biz_id = #{bizId},
            type = #{type},
            content = #{content},
            media_url = #{mediaUrl},
            cover = #{cover},
            view_count = #{viewCount},
            like_count = #{likeCount},
            category = #{category}
        WHERE id = #{id}
    </update>

    <update id="increaseViewCount">
        UPDATE content
        SET view_count = view_count + 1
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM content
        WHERE id = #{id} AND biz_type = 'culture'
    </delete>

</mapper>
